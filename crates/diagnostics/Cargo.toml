[package]
name = "diagnostics"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Locale-agnostic, error-code-tagged diagnostics rendered through Fluent"
readme = "../../README.md"
license.workspace = true
keywords = ["diagnostics", "compiler", "i18n"]
categories = ["compilers"]

[dependencies]
ast = { version = "0.1.0", path = "../ast" }
fluent-bundle = "0.15"
unic-langid = "0.9"

[lints]
workspace = true
