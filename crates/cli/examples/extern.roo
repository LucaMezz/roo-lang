
// This module source code would be generated by the host
mod host {

    struct Vector2<T> {
        x: T,
        y: T,
    }

    impl<T> Vector2<T> {
        fn new(x: T, y: T) -> Self<T> {
            Self {
                x, y
            }
        }
    }

}
