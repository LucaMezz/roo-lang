use crate::value::Value;
use crate::{InstrAddr, StringId};

#[derive(Debug, Clone, Copy)]
pub enum Instr {
    Nop,
    Halt,
    Push(Value),
    Pop,
    Jump(InstrAddr),
    JumpIfFalse(InstrAddr),
    LoadString(StringId),
    Concat,
    Slice(usize, usize),
    Add,
    Sub,
    Mul,
    Div,
    Rem,
    BitXor,
    BitAnd,
    BitOr,
    Shl,
    Shr,
    Not,
    Neg,
    Is,
    Eq,
    Ne,
    Lt,
    Le,
    Gt,
    Ge,
    Dup,
    Array(usize),
    Index,
    IndexMut,
    Insert,
    Remove,
    Len,
    Adt(usize),
    Call,
    Ret,
    GetLocal(usize),
    SetLocal(usize),
    Tag,
}
