# Introduction

> **Work in progress.** This book hasn't been written yet. Content is
> coming.

For reference, here's how a Roo code block is written in this book (fenced
with `roo` so the `mdbook-roo-highlight` preprocessor picks it up):

```roo
fn add(a: int, b: int) -> int {
    a + b
}
```
